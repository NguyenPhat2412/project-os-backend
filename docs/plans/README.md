# Implementation Plans

Every future backend implementation plan belongs in this directory:

```text
YYYY-MM-DD-<short-kebab-case-name>.md
```

Each plan must include goal, architecture, exact files, ordered steps, database
and security impact, verification commands, acceptance criteria, and status.

When a plan is completed, keep it here and update `../memory/project-os-memory.md`.
